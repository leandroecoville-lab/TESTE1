# HOW_TO_DEBUG

- Ver logs do build do docker compose
- Rodar `docker compose ... run --rm pack-factory --help` para validar CLI
- Se merge falhar por `history_append_only_violation`, revisar regra: history/ é append-only.
